name: categories_repository
description: A repository that handles category related requests.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  categories_api:
    path: ../categories_api

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
